<UserControl x:Class="OpenKHS.Controls.ScheduleHeadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenKHS.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="200">
    <Grid IsEnabled="{Binding IsItemSelected}">
        <WrapPanel Margin="10">
            <Label Width="140">Week Starting</Label>
            <TextBox Width="160" Text="{Binding SelectedItem.WeekStarting, Mode=OneWay, StringFormat=yyyy-MM-dd}" 
                         IsReadOnly="True" />
        </WrapPanel>
    </Grid>
</UserControl>
